{
	"scripts": {
		"build": "powershell -NoProfile -Command \"if (Test-Path build) { Remove-Item build -Recurse -Force }; tsc\"",
		"start": "node build/index.js",
		"dev": "npm run build && npm run start"
	},
	"dependencies": {
		"canvas": "^3.2.3",
		"discord.js": "^14.26.4",
		"dotenv": "^17.4.2",
		"skia-canvas": "^3.0.8"
	},
	"devDependencies": {
		"@types/gifencoder": "^2.0.3",
		"typescript": "^5.0.0"
	}
}
